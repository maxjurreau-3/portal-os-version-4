# Portal‑OS v4 Versioning

Portal‑OS v4 uses semantic versioning:

- MAJOR — breaking changes
- MINOR — new features
- PATCH — fixes

Version tags are created by the pipeline:


The Shell UI displays version logs and pipeline state.
runVersion("v0.1.0")
